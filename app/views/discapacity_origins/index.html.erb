<h1>Listing discapacity_origins</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @discapacity_origins.each do |discapacity_origin| %>
  <tr>
    <td><%= discapacity_origin.name %></td>
    <td><%= link_to 'Show', discapacity_origin %></td>
    <td><%= link_to 'Edit', edit_discapacity_origin_path(discapacity_origin) %></td>
    <td><%= link_to 'Destroy', discapacity_origin, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Discapacity origin', new_discapacity_origin_path %>
