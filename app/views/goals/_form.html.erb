<%= form_for(@goal, :url => url, :method => method) do |f| %>
  <% if @goal.errors.any? %>
    <div id="error_explanation">
      <h2>Imposible registrar meta</h2>

      <ul>
      <% @goal.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :month, "Mes" %>
    <%= f.number_field :month, :class => "span7" %>
  </div>
  <div class="field">
    <%= f.label :year, "Año" %>
    <%= f.number_field :year, :class => "span7" %>
  </div>
  <div class="field">
    <%= f.label :minimum, "Mínimo" %>
    <%= f.text_field :minimum, :class => "span7" %>
  </div>
  <div class="field">
    <%= f.label :satisfying, "Satisfactorio" %>
    <%= f.text_field :satisfying, :class => "span7" %>
  </div>
  <div class="field">
    <%= f.label :excelent, "Excelente" %>
    <%= f.text_field :excelent, :class => "span7" %>
  </div>

  <div class="form-actions btn-group">
    <%= f.submit %>
    <%= link_to 'Regresar', critical_success_factor_path(@critical_success_factor), :class => "btn" %>
  </div>
<% end %>
