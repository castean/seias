<%= form_for(@user_session, :html => {:class => "form-stacked"}) do |f| %>
  <% if @user_session.errors.any? %>
    <div id="error_explanation" class="error">
      <h2>Imposible iniciar sesion, datos incorrectos</h2>

      <ul>
      <% @user_session.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>

    </div>
  <% end %>

<fieldset class="inputs">
  <p>Nota:

    A partir del Lunes 10 de Septiembre del 2012, la contraseña de las cuentas para ingresar al sistema va cambiar, por
    la que USTEDES LE PROPORCIONARION A VICTOR VEGA. En caso de que no la recuerden o no se la ubieran pasado favor de
    comunicarse con él para que les asigne una, al teléfono del DIF(214-40-00) ext. 1217 o al 439-15-86 ext. 7557

  </p>
  <div class="clearfix">
    <%= f.label :login,"Usuario" %>
    <div class="input">
      <%= f.text_field :login %>
    </div>
  </div>
  <div class="clearfix">
    <%= f.label :password, "Contraseña" %>
    <div class="input">
      <%= f.password_field :password %>
    </div>
  </div>

  <div class="actions">
    <%= f.submit 'Iniciar sesion' , :class => "btn primary"%>
  </div>
</fieldset>
<% end %>