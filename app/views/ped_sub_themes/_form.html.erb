<%= form_for(@ped_sub_theme) do |f| %>
  <% if @ped_sub_theme.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ped_sub_theme.errors.count, "error") %> prohibited this ped_sub_theme from being saved:</h2>

      <ul>
      <% @ped_sub_theme.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
