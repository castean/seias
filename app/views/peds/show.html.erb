<p id="notice"><%= notice %></p>

<p>
  <b>Descripción:</b></br>
  <%= @ped.description %>
</p>
<br/>
<p>
  <b>Subtemas:</b>
</p>


<% @ped.ped_sub_themes.each do |subtheme| %>
	<ul>
		<li>
			<%= link_to subtheme.name, ped_sub_theme_path(subtheme.id) %>
		</li>
	</ul>
<%end%>
</br>
<% if can? :update, @ped %>
	<%= link_to 'Edit', edit_ped_path(@ped) %> |
<% end %>
<%= link_to 'Back', peds_path %>
