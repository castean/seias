<div class="index">
  <ul>
  <% @users.each do |user| %>
     <li>
       <span><%= user.fullname %></span>
       <span><%= link_to 'Ver Detalles', user %></span>
       <span><%= link_to 'Editar', edit_user_path(user) %></span>
       <span><%= link_to 'Eliminar', user, confirm: '¿Esta seguro?', method: :delete %></span>
     </li>
  <% end %>
  </ul>
</div>

<%= link_to "Registrar Usuario", new_user_path %>