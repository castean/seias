<p id="notice"><%= notice %></p>

<p>
  <b>Objetivo:</b></br>
  <%= @ped_goal.name %>: <%= @ped_goal.description %>
</p>
<br/>
<p>
  <b>Estrategias:</b>
</p>

<% @ped_goal.ped_strategies.each do |strategy| %>
	<ul>
		<li>
			<%= link_to strategy.name, ped_strategy_path(strategy.id)%>: <%= strategy.description%>
		</li>
	</ul>
<%end%>
</br>



<% if can? :update, @ped_goal %>
	<%= link_to 'Edit', edit_ped_goal_path(@ped_goal) %> |
<%end%>
<%= link_to 'Back', ped_goals_path %>
