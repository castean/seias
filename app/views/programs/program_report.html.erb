<h3>Lista de Programas</h3>

<table class="table table-striped">
  <thead>
	  <tr>

	    <th>Programa</th>

	  </tr>
	</thead>

  <tr>
  	<% c = Program.find(params[:program_id][:program_id]) %>
    <td><%= c.description %></td>

  </tr>

</table>
<table class="table table-striped">
  <thead>
	  <tr>
	  	<th>Tipo de Actividad</th>
	    <th>Valor</th>
	    <th>Hombres</th>
	    <th>Mujeres</th>
	    <th>Total Personas</th>
	    <th><%=  %></th>
	  </tr>
	</thead>

<% @program.each do |p| %>

  <tr>
  	<td><%= p.nombre%></td>
  	<td><%= p.totalv%></td>
  	<td><%= p.hombre%></td>
  	<td><%= p.mujer%></td>
  	<td><%= p.totalp%></td>
  </tr>
<%end%>
</table>
<br/>

<%=  link_to 'Descargar en excel', request.parameters.merge({:format => :xls}), :controller => 'programs', :action => 'program_report' %>