<%= form_for(@funcional_support) do |f| %>
  <% if @funcional_support.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@funcional_support.errors.count, "error") %> prohibited this funcional_support from being saved:</h2>

      <ul>
      <% @funcional_support.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
