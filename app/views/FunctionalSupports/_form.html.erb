<%= form_for(@functionalsupport) do |f| %>
  <% if @functionalsupport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@functionalsupport.errors.count, "error") %> prohibited this funcional_support from being saved:</h2>

      <ul>
      <% @functionalsupport.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
