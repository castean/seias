<%= form_for(@priority_program) do |f| %>
  <% if @priority_program.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@priority_program.errors.count, "error") %> prohibited this priority_program from being saved:</h2>

      <ul>
      <% @priority_program.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<br />
  <div class="field">
    <%= f.label :name, "Nombre" %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description, "Descripción" %>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :date_start, "Fecha de Inicio" %>
    <%= f.date_select :date_start %>
  </div>
  <div class="field">
    <%= f.label :date_end, "Fecha de Termino" %>
    <%= f.date_select :date_end %>
  </div><br />
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
