<%= form_for(@number_of_room) do |f| %>
  <% if @number_of_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@number_of_room.errors.count, "error") %> prohibited this number_of_room from being saved:</h2>

      <ul>
      <% @number_of_room.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :point %><br />
    <%= f.number_field :point %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
