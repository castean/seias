<h1>Indicadores</h1>
</br>
<table>
  <tr>
    <th>Tipo de Indicador</th>
    <th>Nombre</th>
    <th>Descripción</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @critical_factors.each do |critical_factor| %>
  <tr>
    <td><%= critical_factor.critical_success_factor_type.name %></td>
    <td><%= critical_factor.name %></td>
    <td><%= critical_factor.description %></td>
    <td><%= link_to 'Show', critical_factor %></td>
    <td><%= link_to 'Edit', edit_critical_factor_path(critical_factor) %></td>
    <td><%= link_to 'Destroy', critical_factor, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nuevo Indicador', new_critical_factor_path %>
