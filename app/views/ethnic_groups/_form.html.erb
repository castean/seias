<%= form_for(@ethnic_group) do |f| %>
  <% if @ethnic_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ethnic_group.errors.count, "error") %> prohibited this ethnic_group from being saved:</h2>

      <ul>
      <% @ethnic_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
