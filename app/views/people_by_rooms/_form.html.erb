<%= form_for(@people_by_room) do |f| %>
  <% if @people_by_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@people_by_room.errors.count, "error") %> prohibited this people_by_room from being saved:</h2>

      <ul>
      <% @people_by_room.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :point %><br />
    <%= f.number_field :point %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
