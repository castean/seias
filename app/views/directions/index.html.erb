<h1>Listado de  Direcciones</h1>

<table>
  <tr>
  	<th>Nombre</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @directions.each do |direction| %>
  <tr>
  	<td><%= direction.name %></td>
    <td><%= link_to 'Ir a Departamentos', direction %></td>
    <td><%= link_to 'Editar', edit_direction_path(direction) %></td>
    <td><%= link_to 'Destruir', direction, confirm: '¿Esta Seguro?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nueva Direccion', new_direction_path %>
